import React from 'react';

const TeamDetails = ({ team }) => {
  // Render team details
  return (
    <div>
      <h2>Team Details</h2>
      {/* Display team information */}
    </div>
  );
};

export default TeamDetails;